{
  "name": "hello",
  "version": "1.0.0",
  "description": "",
  "main": "lambda.ts",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.17.18",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/lambda.ts --bundle --platform=node --outfile=dist/lambda.js"
  },
  "author": "",
  "license": "ISC"
}
